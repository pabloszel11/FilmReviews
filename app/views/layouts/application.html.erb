<!DOCTYPE html>
<html>
  <head>
    <title>Films</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class = "navbar navbar-inverse">

      <div class = "container">
        <div class = "navbar-header">
          <%= link_to "Films", root_path, class: "navbar-brand" %>
        </div>

        <ul class = "nav navbar-nav">
          <li><%= link_to "Sign up", new_user_registration_path  %></li>
          <% if user_signed_in? %>
            <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to "Log in", new_user_session_path %></li>
          <% end %>
          <li></li>
        </ul>

        <ul class = "nav navbar-nav navbar-right">
          <li class = "dropdown">
            <a href ="#" class = "dropdown-toggle" data-toggle = "dropdown" role = "button" aria-expanded =" false">Genres <span class = "caret"></a>
            <ul class = "dropdown-menu" role = "menu">
              <% Genre.all.each do |genre| %>
                <li>
                  <%= link_to genre.name, films_path(genre: genre.name), class: "link" %>
                </li>
              <% end %>
            </ul>
          </li>

          <% if user_signed_in? %>
          <li><%= link_to "Add film", new_film_path %></li>
          <% end %>
        </ul>
      </div>



    </nav>

  	<p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <%= yield %>
  </body>
</html>
